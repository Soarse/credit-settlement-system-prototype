# 信贷资方结算系统原型 · 完善说明

本目录是在参考原型副本上完成的改进版，原参考工程保持不变。改进版仍为无后端、无外部依赖的前端模拟工程，业务数据在页面刷新或“恢复基准数据”后重置。

## 本轮已完善

- **部分到账**：按实际到账金额生成结算流水和银行回单，结算单进入“部分结算”，保留已收与剩余应收，可继续认领后续到账。
- **部分争议**：金额争议只冻结争议部分，无争议金额仍可进入结算；账单与结算详情同时展示已结、争议和可结金额。
- **五级勾稽**：事件终态与费用流水分别取数；缺失流水会被识别。所有金额等式的左右侧独立展示，失败时可直接看到差额。
- **结算边界**：结算单按资金方、协议结算账户、计划结算日和币种分组，防止跨账户、跨日期或跨币种合并。
- **金额与税额精度**：核心金额运算改为十进制定点方式；账单明细按费用项和税率拆分，再汇总不含税金额与税额，支持混合税率。
- **角色工作台**：运营大盘增加账期选择、统计口径和当前业务日，并按当前角色给出可处理任务。
- **审批审计**：审批单同时记录具名用户与角色，固化审批内容版本、内容指纹和系统证据快照；界面明确内容变更后须重新审批。
- **可复现验收**：新增“验收场景”，可一键执行部分到账、部分争议和流水缺失三类高风险场景，并跳转业务页核对结果。
- **全流程新手引导**：右上角一键开启或退出 18 步蒙版教程，自动跨模块导航；每一步分别说明业务逻辑、应用系统操作和必须记住的控制点，支持全流程目录、键盘切换、断点续学与完成后重学。
- **可选流程引导**：在“新手引导”旁新增独立的“流程引导”，提供 30 个标准业务流程和 2 个跨模块综合演练；支持关键词搜索、阶段筛选、完成标记、断点续学和流程切换。点击具体流程后自动跳转相关页面并启动蒙版，每一步分别说明业务流程、应用操作流程和检查点。
- **引导顶部快捷翻页**：新手引导和流程引导均在页面顶部中央增加同步的“上一步／下一步”按钮，减少连续学习时的鼠标移动；原引导卡片底部按钮、样式和内容保持不变。
- **菜单能力提示**：鼠标悬浮或键盘聚焦左侧任一功能菜单时，展示该模块的主要能力、能力归并说明和相关外部系统；模块标题严格使用左侧栏现有名称。

## 自动校验

运行 `review-checks.cjs` 会校验：十进制金额精度、全流程引导覆盖度、32 个可选流程引导的编号与双流程说明完整性、审批证据、基准账期勾稽、流水缺失识别、勾稽左右值独立、部分到账、部分争议和结算分组边界。

## 启动

双击 `启动原型.cmd` 会自动启动本地服务并打开页面。也可以使用 Node.js 运行 `server.cjs`，默认访问地址为 `http://127.0.0.1:8081`。
